#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias robeet=reboot
alias poweron='echo what did you expect that to accomplish exactly?'
alias vim=nvim
alias v=vim
